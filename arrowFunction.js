'use strict'

function arrow1(params) {
        return params + 2; 
    }
//that function is regular function that will return 4

var arrow2 = (params) => params + 2 
//this is **arrow function** which have the same result as **arrow1** function above
//but with sorter syntax
